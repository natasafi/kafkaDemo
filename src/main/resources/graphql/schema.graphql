type Query{
    user: [User]
}

type User {
    id: ID!
    name: String
    age: Int
}

type Mutation {
    newUser(name: String!, age: Int) : User!
    deleteUser(id: ID!) : Boolean
    updateUser(id:ID!, age: Int) : User!
}